<link rel="stylesheet" href="{{ theme_base }}/dist/main.css">
    <style>
        /* 依赖 theme_base 的样式（SCSS 编译期无法使用模板变量） */
        .hero{background-image:url("{{ theme_base }}/assets/hero.svg")}
    </style>
